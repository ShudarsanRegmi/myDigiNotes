# File Handling in C++

- fstream library is used to work with files in c++

Three classes in fastream library are used to create, write or read files.
```
  # classes
  ofstream -> creates and writes to a file
  ifstream -> reads from files
  fstream -> A cmobination of ofstream and ifstrea: creates, reads, and writes to files 
```

### Creating and writing to a file using `ofstream`
```cpp

```
